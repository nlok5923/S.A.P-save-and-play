<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <title>playlist</title>
</head>
<body> 
    <h3 class="text-center mt-5">Tap the button next correspoding to your song to play it</h3>
    <div class="container text-center">
        <% if(length == 0){ %> 
        <h4>Bro add some songs yaar</h1>
            <%} else {%>
        <% arr.map((data)=>{ %>
        <form method="GET" action="/track/<%= data.id %> ">
        <button type="submit" class="btn btn-primary mt-3">play <%= data.name %></button>
        </form>
         <% }) }%>
    </div>
 </body>
</html>